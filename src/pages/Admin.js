export const Admin = () => {
  return (
    <main>
      <div className="component">Welcome to the Admin Page</div>
    </main>
    // We put the Admin component inside a main tag to semantically indicate that this is the main content of a page and not a component that is part of the header or footer. This is important for accessibility and SEO purposes.
  )
}
